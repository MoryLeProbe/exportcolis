{% extends 'base.html.twig' %}

{% block title %}Modifier Tarif de Port{% endblock %}

{% block body %}
<h2 class="mb-4">Modifier un Tarif de Port</h2>

{{ form_start(form) }}
    <div class="card p-4 shadow-sm">
        {{ form_widget(form) }}
        <button class="btn btn-success mt-3">
            <i class="bi bi-check-circle"></i> Enregistrer
        </button>
        <a href="{{ path('app_tarif_port_index') }}" class="btn btn-secondary mt-3">Retour</a>
    </div>
{{ form_end(form) }}

{% endblock %}
